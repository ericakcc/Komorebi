---
name: project-manager
description: |
  專案管理與任務追蹤技能。在以下情況會自動觸發：
  - 詢問專案狀態、進度、統計（如「專案進度如何？」）
  - 需要新增、完成、查看任務（如「幫我加一個任務」）
  - 詢問今日待辦（如「我今天該做什麼？」）
  - 專案同步或更新（如「同步 Komorebi 專案」）
allowed-tools:
  - mcp__project__list_projects
  - mcp__project__show_project
  - mcp__project__get_today_tasks
  - mcp__project__sync_project
  - Read
  - Edit
---

# Project Manager Skill

這是 Komorebi 的專案管理技能，提供任務追蹤和專案進度管理功能。

## 資料結構

專案使用**資料夾模式**儲存在 `data/projects/` 目錄：

```
data/projects/
├── komorebi/                    # 專案資料夾
│   ├── project.md               # 專案元資料 + 進度日誌
│   ├── tasks.md                 # 任務清單
│   └── notes/                   # 筆記（可選）
│
├── layerwise/
│   ├── project.md
│   ├── tasks.md
│   └── notes/
```

### project.md 格式

```yaml
---
name: Komorebi
type: software           # software | research | learning
status: active           # active | paused | completed | archived
priority: 1
created: 2026-01-14
updated: 2026-01-18      # 自動更新
repo: ~/Komorebi         # 軟體專案才需要
progress: 65             # 百分比（自動計算）
stats:
  total_tasks: 15
  completed_tasks: 10
  commits_this_week: 8
---

# Komorebi

## 目標
...

## 技術棧
...

## 進度日誌
### 2026-01-18
- 完成功能 A
```

### tasks.md 格式

```markdown
## In Progress
- [ ] 完成 calendar 整合 #mvp-4 @today
- [ ] 規劃 hooks 架構

## Pending
- [ ] 實作 PreToolUse hook

## Completed
- [x] 實作 list_projects 工具 (2026-01-14)
```

**任務標記說明**：
- `@today` - 今日任務，會被 `get_today_tasks` 工具收集
- `#tag` - 任務標籤，用於分類（如 #mvp-4, #bug）
- `(YYYY-MM-DD)` - 完成日期，記錄在已完成任務後

---

## MCP 工具說明

### list_projects
列出所有專案及統計摘要。

**輸出範例**：
```
## 專案列表

- 🟢 📁 **Komorebi** (active) | 10/15 tasks, 65%
- 🟢 📁 **LayerWise** (active) | 5/20 tasks, 25%
```

### show_project
顯示專案詳細資訊（包含 project.md 和 tasks.md）。

**參數**：
- `name`: 專案名稱（不分大小寫）

### get_today_tasks
取得今日任務清單（跨所有專案）。掃描所有 tasks.md 中標記 `@today` 的任務。

**輸出範例**：
```
## 今日任務

### @today 標記
- [ ] **komorebi**: 完成 calendar 整合 #mvp-4

### 進行中
- [ ] **layerwise**: 實作 VLA 模組
```

### sync_project
從 repo 同步專案資訊，用 AI 分析 README 和程式碼結構。

**參數**：
- `name`: 專案名稱
- `force`: 是否強制覆寫（預設 false）

---

## 任務管理指引

當用戶要求新增、完成或修改任務時，**直接編輯 tasks.md** 檔案，而非使用專門的工具。

### 新增任務

1. 使用 `Read` 工具讀取 `data/projects/<project>/tasks.md`
2. 根據任務性質，在適當的區塊新增：
   - 緊急任務 → In Progress
   - 一般任務 → Pending
3. 使用 `Edit` 工具寫入變更

**範例**：
```
用戶：幫我加一個任務「完成 API 整合」到 Komorebi

步驟：
1. Read data/projects/komorebi/tasks.md
2. Edit: 在 ## Pending 區塊新增 "- [ ] 完成 API 整合"
```

### 完成任務

1. 讀取 tasks.md
2. 找到任務，將 `[ ]` 改為 `[x]`
3. 將任務移動到 ## Completed 區塊
4. 加上完成日期 `(YYYY-MM-DD)`

**範例**：
```markdown
# Before (In Progress)
- [ ] 完成 API 整合

# After (Completed)
- [x] 完成 API 整合 (2026-01-18)
```

### 標記今日任務

在任務後加上 `@today` 標記：

```markdown
- [ ] 完成 API 整合 @today
```

---

## 專案類型處理

### software 類型
- 有 `repo` 欄位指向程式碼目錄
- 可使用 `sync_project` 同步 README 資訊
- 進度可從 git commits 推算

### research 類型
- 用於論文閱讀、研究專案
- 資料夾可包含 `papers/`（PDF）和 `implementations/`（實作程式碼）
- 進度由任務完成率計算

### learning 類型
- 用於學習筆記、課程追蹤
- 進度由任務完成率計算

---

## 統計計算方式

### 進度百分比 (progress)
```
progress = (completed_tasks / total_tasks) * 100
```

### 任務統計 (stats)
- `total_tasks`: In Progress + Pending + Completed 的總數
- `completed_tasks`: Completed 區塊的任務數
- `commits_this_week`: 過去 7 天的 git commits 數（僅 software 類型）

---

## 最佳實踐

1. **每日開始時**：使用 `get_today_tasks` 查看今日待辦
2. **每日結束時**：更新任務狀態，將完成的任務標記為 done
3. **新增任務時**：評估優先順序，適當使用標籤分類
4. **專案同步**：定期使用 `sync_project` 更新專案描述和進度
